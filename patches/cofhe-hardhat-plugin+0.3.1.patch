diff --git a/node_modules/cofhe-hardhat-plugin/dist/src/addresses.d.ts b/node_modules/cofhe-hardhat-plugin/dist/src/addresses.d.ts
index 097fc4c..adbe488 100644
--- a/node_modules/cofhe-hardhat-plugin/dist/src/addresses.d.ts
+++ b/node_modules/cofhe-hardhat-plugin/dist/src/addresses.d.ts
@@ -1,5 +1,5 @@
 export declare const TASK_MANAGER_ADDRESS = "0xeA30c4B8b44078Bbf8a6ef5b9f1eC1626C7848D9";
-export declare const ZK_VERIFIER_ADDRESS = "0x0000000000000000000000000000000000000100";
+export declare const ZK_VERIFIER_ADDRESS = "0x0000000000000000000000000000000000000101";
 export declare const QUERY_DECRYPTER_ADDRESS = "0x0000000000000000000000000000000000000200";
 export declare const TEST_BED_ADDRESS = "0x0000000000000000000000000000000000000300";
 //# sourceMappingURL=addresses.d.ts.map
\ No newline at end of file
diff --git a/node_modules/cofhe-hardhat-plugin/dist/src/addresses.js b/node_modules/cofhe-hardhat-plugin/dist/src/addresses.js
index f507dd3..76a5462 100644
--- a/node_modules/cofhe-hardhat-plugin/dist/src/addresses.js
+++ b/node_modules/cofhe-hardhat-plugin/dist/src/addresses.js
@@ -2,7 +2,7 @@
 Object.defineProperty(exports, "__esModule", { value: true });
 exports.TEST_BED_ADDRESS = exports.QUERY_DECRYPTER_ADDRESS = exports.ZK_VERIFIER_ADDRESS = exports.TASK_MANAGER_ADDRESS = void 0;
 exports.TASK_MANAGER_ADDRESS = "0xeA30c4B8b44078Bbf8a6ef5b9f1eC1626C7848D9";
-exports.ZK_VERIFIER_ADDRESS = "0x0000000000000000000000000000000000000100";
+exports.ZK_VERIFIER_ADDRESS = "0x0000000000000000000000000000000000000101";
 exports.QUERY_DECRYPTER_ADDRESS = "0x0000000000000000000000000000000000000200";
 exports.TEST_BED_ADDRESS = "0x0000000000000000000000000000000000000300";
 //# sourceMappingURL=addresses.js.map
\ No newline at end of file
diff --git a/node_modules/cofhe-hardhat-plugin/dist/src/compile-mock-contracts.js b/node_modules/cofhe-hardhat-plugin/dist/src/compile-mock-contracts.js
index 2a6c5f8..1662166 100644
--- a/node_modules/cofhe-hardhat-plugin/dist/src/compile-mock-contracts.js
+++ b/node_modules/cofhe-hardhat-plugin/dist/src/compile-mock-contracts.js
@@ -62,7 +62,7 @@ const compileMockContractPaths = async (hre) => {
         await hre.run("compile:silent");
     }
     finally {
-        fs_1.default.rmSync(directory, { recursive: true });
+        fs_1.default.rmSync(directory, { recursive: true, force: true });
     }
 };
 exports.compileMockContractPaths = compileMockContractPaths;
diff --git a/node_modules/cofhe-hardhat-plugin/dist/src/deploy-mocks.js b/node_modules/cofhe-hardhat-plugin/dist/src/deploy-mocks.js
index 6907ea8..88a6ef7 100644
--- a/node_modules/cofhe-hardhat-plugin/dist/src/deploy-mocks.js
+++ b/node_modules/cofhe-hardhat-plugin/dist/src/deploy-mocks.js
@@ -9,17 +9,18 @@ const addresses_1 = require("./addresses");
 const compile_mock_contracts_1 = require("./compile-mock-contracts");
 const chalk_1 = __importDefault(require("chalk"));
 const const_1 = require("./const");
-// Deploy
+// Deploy via factory then copy runtime code to fixed address (viaIR compatible)
 const deployMockTaskManager = async (hre) => {
     const [signer] = await hre.ethers.getSigners();
-    // Deploy MockTaskManager
-    const TaskManagerArtifact = await hre.artifacts.readArtifact("TaskManager");
-    await (0, utils_1.hardhatSetCode)(hre, addresses_1.TASK_MANAGER_ADDRESS, TaskManagerArtifact.deployedBytecode);
+    const factory = await hre.ethers.getContractFactory("TaskManager");
+    const tempTM = await factory.deploy();
+    await tempTM.waitForDeployment();
+    const tempAddr = await tempTM.getAddress();
+    const runtimeCode = await hre.ethers.provider.getCode(tempAddr);
+    await (0, utils_1.hardhatSetCode)(hre, addresses_1.TASK_MANAGER_ADDRESS, runtimeCode);
     const taskManager = await hre.ethers.getContractAt("TaskManager", addresses_1.TASK_MANAGER_ADDRESS);
-    // Initialize MockTaskManager
     const initTx = await taskManager.initialize(signer.address);
     await initTx.wait();
-    // Check if MockTaskManager exists
     const tmExists = await taskManager.exists();
     if (!tmExists) {
         throw new Error("MockTaskManager does not exist");
@@ -27,13 +28,10 @@ const deployMockTaskManager = async (hre) => {
     return taskManager;
 };
 const deployMockACL = async (hre) => {
-    // Get Signer
     const [signer] = await hre.ethers.getSigners();
-    // Deploy ACL implementation
     const aclFactory = await hre.ethers.getContractFactory("ACL");
     const acl = await aclFactory.deploy(signer.address);
     await acl.waitForDeployment();
-    // Check if ACL exists
     const exists = await acl.exists();
     if (!exists) {
         (0, utils_1.logError)("ACL does not exist", 2);
@@ -42,8 +40,12 @@ const deployMockACL = async (hre) => {
     return acl;
 };
 const deployMockZkVerifier = async (hre) => {
-    const zkVerifierArtifact = await hre.artifacts.readArtifact("MockZkVerifier");
-    await (0, utils_1.hardhatSetCode)(hre, addresses_1.ZK_VERIFIER_ADDRESS, zkVerifierArtifact.deployedBytecode);
+    const factory = await hre.ethers.getContractFactory("MockZkVerifier");
+    const tempZkV = await factory.deploy();
+    await tempZkV.waitForDeployment();
+    const tempAddr = await tempZkV.getAddress();
+    const runtimeCode = await hre.ethers.provider.getCode(tempAddr);
+    await (0, utils_1.hardhatSetCode)(hre, addresses_1.ZK_VERIFIER_ADDRESS, runtimeCode);
     const zkVerifier = await hre.ethers.getContractAt("MockZkVerifier", addresses_1.ZK_VERIFIER_ADDRESS);
     const zkVerifierExists = await zkVerifier.exists();
     if (!zkVerifierExists) {
@@ -53,13 +55,15 @@ const deployMockZkVerifier = async (hre) => {
     return zkVerifier;
 };
 const deployMockQueryDecrypter = async (hre, acl) => {
-    const queryDecrypterArtifact = await hre.artifacts.readArtifact("MockQueryDecrypter");
-    await (0, utils_1.hardhatSetCode)(hre, addresses_1.QUERY_DECRYPTER_ADDRESS, queryDecrypterArtifact.deployedBytecode);
+    const factory = await hre.ethers.getContractFactory("MockQueryDecrypter");
+    const tempQD = await factory.deploy();
+    await tempQD.waitForDeployment();
+    const tempAddr = await tempQD.getAddress();
+    const runtimeCode = await hre.ethers.provider.getCode(tempAddr);
+    await (0, utils_1.hardhatSetCode)(hre, addresses_1.QUERY_DECRYPTER_ADDRESS, runtimeCode);
     const queryDecrypter = await hre.ethers.getContractAt("MockQueryDecrypter", addresses_1.QUERY_DECRYPTER_ADDRESS);
-    // Initialize MockQueryDecrypter
     const initTx = await queryDecrypter.initialize(addresses_1.TASK_MANAGER_ADDRESS, await acl.getAddress());
     await initTx.wait();
-    // Check if MockQueryDecrypter exists
     const queryDecrypterExists = await queryDecrypter.exists();
     if (!queryDecrypterExists) {
         (0, utils_1.logError)("MockQueryDecrypter does not exist", 2);
@@ -68,8 +72,12 @@ const deployMockQueryDecrypter = async (hre, acl) => {
     return queryDecrypter;
 };
 const deployTestBedContract = async (hre) => {
-    const testBedFactory = await hre.artifacts.readArtifact("TestBed");
-    await (0, utils_1.hardhatSetCode)(hre, addresses_1.TEST_BED_ADDRESS, testBedFactory.deployedBytecode);
+    const factory = await hre.ethers.getContractFactory("TestBed");
+    const tempTB = await factory.deploy();
+    await tempTB.waitForDeployment();
+    const tempAddr = await tempTB.getAddress();
+    const runtimeCode = await hre.ethers.provider.getCode(tempAddr);
+    await (0, utils_1.hardhatSetCode)(hre, addresses_1.TEST_BED_ADDRESS, runtimeCode);
     const testBed = await hre.ethers.getContractAt("TestBed", addresses_1.TEST_BED_ADDRESS);
     await testBed.waitForDeployment();
     return testBed;
@@ -92,7 +100,6 @@ const deployMocks = async (hre, options = {
     gasWarning: true,
     silent: false,
 }) => {
-    // Check if network is Hardhat, if not log skip message and return
     const isHardhat = await (0, utils_1.checkNetworkAndSkip)(hre);
     if (!isHardhat)
         return;
@@ -116,15 +123,12 @@ const deployMocks = async (hre, options = {
             (0, utils_1.logWarning)(message, indent);
         }
     };
-    // Log start message
     logEmptyIfNoisy();
     logSuccessIfNoisy(chalk_1.default.bold("cofhe-hardhat-plugin :: deploy mocks"), 0);
     logEmptyIfNoisy();
-    // Compile mock contracts
     await (0, compile_mock_contracts_1.compileMockContractPaths)(hre);
     logEmptyIfNoisy();
     logSuccessIfNoisy("Mock contracts compiled", 1);
-    // Deploy mock contracts
     const taskManager = await deployMockTaskManager(hre);
     logDeploymentIfNoisy("MockTaskManager", await taskManager.getAddress());
     const acl = await deployMockACL(hre);
@@ -144,10 +148,8 @@ const deployMocks = async (hre, options = {
         const testBed = await deployTestBedContract(hre);
         logDeploymentIfNoisy("TestBed", await testBed.getAddress());
     }
-    // Log success message
     logEmptyIfNoisy();
     logSuccessIfNoisy(chalk_1.default.bold("cofhe-hardhat-plugin :: mocks deployed successfully"), 0);
-    // Log warning about mocks increased gas costs
     if (options.gasWarning) {
         logEmptyIfNoisy();
         logWarningIfNoisy("When using mocks, FHE operations (eg FHE.add / FHE.mul) report a higher gas price due to additional on-chain mocking logic. Deploy your contracts on a testnet chain to check the true gas costs.\n(Disable this warning by setting 'cofhejs.gasWarning' to false in your hardhat config", 0);
@@ -155,4 +157,3 @@ const deployMocks = async (hre, options = {
     logEmptyIfNoisy();
 };
 exports.deployMocks = deployMocks;
-//# sourceMappingURL=deploy-mocks.js.map
\ No newline at end of file
diff --git a/node_modules/cofhe-hardhat-plugin/src/addresses.ts b/node_modules/cofhe-hardhat-plugin/src/addresses.ts
index d8b2769..070ea74 100644
--- a/node_modules/cofhe-hardhat-plugin/src/addresses.ts
+++ b/node_modules/cofhe-hardhat-plugin/src/addresses.ts
@@ -1,6 +1,6 @@
 export const TASK_MANAGER_ADDRESS =
   "0xeA30c4B8b44078Bbf8a6ef5b9f1eC1626C7848D9";
-export const ZK_VERIFIER_ADDRESS = "0x0000000000000000000000000000000000000100";
+export const ZK_VERIFIER_ADDRESS = "0x0000000000000000000000000000000000000101";
 export const QUERY_DECRYPTER_ADDRESS =
   "0x0000000000000000000000000000000000000200";
 export const TEST_BED_ADDRESS = "0x0000000000000000000000000000000000000300";
diff --git a/node_modules/cofhe-hardhat-plugin/src/compile-mock-contracts.ts b/node_modules/cofhe-hardhat-plugin/src/compile-mock-contracts.ts
index 0635803..d856d44 100644
--- a/node_modules/cofhe-hardhat-plugin/src/compile-mock-contracts.ts
+++ b/node_modules/cofhe-hardhat-plugin/src/compile-mock-contracts.ts
@@ -82,7 +82,7 @@ export const compileMockContractPaths = async (
   try {
     await hre.run("compile:silent");
   } finally {
-    fs.rmSync(directory, { recursive: true });
+    fs.rmSync(directory, { recursive: true, force: true });
   }
 };
 
diff --git a/node_modules/cofhe-hardhat-plugin/src/deploy-mocks.ts b/node_modules/cofhe-hardhat-plugin/src/deploy-mocks.ts
index b20f81c..f9b613f 100644
--- a/node_modules/cofhe-hardhat-plugin/src/deploy-mocks.ts
+++ b/node_modules/cofhe-hardhat-plugin/src/deploy-mocks.ts
@@ -24,13 +24,15 @@ import { MOCK_ZK_VERIFIER_SIGNER_ADDRESS } from "./const";
 const deployMockTaskManager = async (hre: HardhatRuntimeEnvironment) => {
   const [signer] = await hre.ethers.getSigners();
 
-  // Deploy MockTaskManager
-  const TaskManagerArtifact = await hre.artifacts.readArtifact("TaskManager");
-  await hardhatSetCode(
-    hre,
-    TASK_MANAGER_ADDRESS,
-    TaskManagerArtifact.deployedBytecode,
-  );
+  // Deploy TaskManager via factory, then copy runtime code to fixed address.
+  const factory = await hre.ethers.getContractFactory("TaskManager");
+  const tempTM = await factory.deploy();
+  await tempTM.waitForDeployment();
+
+  const tempAddr = await tempTM.getAddress();
+  const runtimeCode = await hre.ethers.provider.getCode(tempAddr);
+
+  await hardhatSetCode(hre, TASK_MANAGER_ADDRESS, runtimeCode);
   const taskManager = await hre.ethers.getContractAt(
     "TaskManager",
     TASK_MANAGER_ADDRESS,
@@ -69,12 +71,16 @@ const deployMockACL = async (hre: HardhatRuntimeEnvironment) => {
 };
 
 const deployMockZkVerifier = async (hre: HardhatRuntimeEnvironment) => {
-  const zkVerifierArtifact = await hre.artifacts.readArtifact("MockZkVerifier");
-  await hardhatSetCode(
-    hre,
-    ZK_VERIFIER_ADDRESS,
-    zkVerifierArtifact.deployedBytecode,
-  );
+  // Deploy MockZkVerifier via factory, then copy its code to the fixed address.
+  // This ensures constructor logic runs and viaIR-compiled bytecode is correct.
+  const factory = await hre.ethers.getContractFactory("MockZkVerifier");
+  const tempZkVerifier = await factory.deploy();
+  await tempZkVerifier.waitForDeployment();
+
+  const tempAddr = await tempZkVerifier.getAddress();
+  const runtimeCode = await hre.ethers.provider.getCode(tempAddr);
+
+  await hardhatSetCode(hre, ZK_VERIFIER_ADDRESS, runtimeCode);
   const zkVerifier = await hre.ethers.getContractAt(
     "MockZkVerifier",
     ZK_VERIFIER_ADDRESS,
@@ -93,14 +99,15 @@ const deployMockQueryDecrypter = async (
   hre: HardhatRuntimeEnvironment,
   acl: Contract,
 ) => {
-  const queryDecrypterArtifact = await hre.artifacts.readArtifact(
-    "MockQueryDecrypter",
-  );
-  await hardhatSetCode(
-    hre,
-    QUERY_DECRYPTER_ADDRESS,
-    queryDecrypterArtifact.deployedBytecode,
-  );
+  // Deploy via factory, then copy runtime code to fixed address
+  const factory = await hre.ethers.getContractFactory("MockQueryDecrypter");
+  const tempQD = await factory.deploy();
+  await tempQD.waitForDeployment();
+
+  const tempAddr = await tempQD.getAddress();
+  const runtimeCode = await hre.ethers.provider.getCode(tempAddr);
+
+  await hardhatSetCode(hre, QUERY_DECRYPTER_ADDRESS, runtimeCode);
   const queryDecrypter = await hre.ethers.getContractAt(
     "MockQueryDecrypter",
     QUERY_DECRYPTER_ADDRESS,
@@ -124,8 +131,14 @@ const deployMockQueryDecrypter = async (
 };
 
 const deployTestBedContract = async (hre: HardhatRuntimeEnvironment) => {
-  const testBedFactory = await hre.artifacts.readArtifact("TestBed");
-  await hardhatSetCode(hre, TEST_BED_ADDRESS, testBedFactory.deployedBytecode);
+  const factory = await hre.ethers.getContractFactory("TestBed");
+  const tempTB = await factory.deploy();
+  await tempTB.waitForDeployment();
+
+  const tempAddr = await tempTB.getAddress();
+  const runtimeCode = await hre.ethers.provider.getCode(tempAddr);
+
+  await hardhatSetCode(hre, TEST_BED_ADDRESS, runtimeCode);
   const testBed = await hre.ethers.getContractAt("TestBed", TEST_BED_ADDRESS);
   await testBed.waitForDeployment();
   return testBed;
